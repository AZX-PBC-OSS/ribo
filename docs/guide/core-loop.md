# The Core Loop

Capture → transcribe → extract → review → write. The headless `@azx/ribo-core` engine drives each step
as a pluggable seam, and the durable outbox carries work across the offline boundary.

::: info Content coming
This page will walk the loop end to end with real code from the packages. For now, the seams are
documented in [Capabilities](/capabilities/) and the [API Reference](/reference/).
:::
