# The Service Worker

The service worker caches the app shell so a returning auditor boots with no network. It is
origin-global and therefore **app-owned** — no package under `packages/` registers, ships, or assumes
one.

::: info Content coming
Full narrative lands in the content pass.
:::
