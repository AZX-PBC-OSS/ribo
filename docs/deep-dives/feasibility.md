# Feasibility

On-device WASM Whisper (`@huggingface/transformers` + `whisper-base.en`) was chosen by attrition — the
other browser-runnable candidates did not survive the spike.

::: info Content coming
Repackaged from the internal feasibility findings in the content pass.
:::
