{
  "schemaVersion": "snuggpro-51-leaf-v2",
  "transcript": "transcripts/11-ambiguous-attic.txt",
  "stresses": "THE WORKED EXAMPLE (see ANNOTATOR_GUIDE.md). SELF-CORRECTION / last-value-wins on two leaves: the attic R-value (\"R-11, no wait ... R-13\" -> 13, 11 retracted) and the heating equipment (\"I thought boiler at first ... no, it's a furnace\" -> furnace, boiler retracted). A health-matrix trap: a personal CO monitor that \"never went off\" is explicitly NOT a real test -> healthAmbientCarbonMonoxide is asserted 'Not Tested', and a model that reads the clean monitor as 'Passed' is a hallucinated pass, not a correct clean. Three blanket disclaimers, recorded as DATA (span + mechanical scope) rather than resolved by annotator judgment — see `disclaimers` below and disclaimer-policy.mjs. And a genuinely new ambiguity the SCHEMA ITSELF introduces, not the transcript: hvacSystemEquipmentType conflates a furnace's identity with an unrelated shared-ducts-with-AC fact this auditor never addresses — recorded as noFittingMember with two symmetric sanctioned alternatives, not resolved by annotator preference.",
  "disclaimers": [
    {
      "span": "no water heater",
      "topic": "the water heater",
      "scope": { "kind": "group", "group": "dhw" }
    },
    {
      "span": "no blower door",
      "topic": "the blower-door test",
      "scope": { "kind": "namedSet", "set": "blowerDoor" }
    },
    {
      "span": "No tests",
      "topic": "the health & safety test battery",
      "scope": { "kind": "namedSet", "set": "healthTests" }
    }
  ],
  "leaves": {
    "hvac.hvacSystemEquipmentType": {
      "status": "asserted",
      "sourceSpan": "Definitely a furnace, scratch the boiler.",
      "member": "Furnace with standalone ducts",
      "retracted": [
        {
          "value": "Boiler",
          "note": "\"I thought boiler at first because of all the pipes\" is withdrawn by \"no, it's a furnace ... those pipes were just the old abandoned hydronic ... scratch the boiler.\" Last assertion wins (annotator guide rule 5)."
        }
      ],
      "arguable": {
        "acceptableAlternatives": ["Furnace / Central AC (shared ducts)"],
        "note": "SCHEMA FINDING, not a transcript ambiguity — escalated in the report rather than resolved by annotator preference. schema.ts's hvacSystemEquipmentType conflates a furnace's IDENTITY with an unrelated fact (whether it shares ducts with a central AC unit) into one enum axis. This utterance states only the identity (\"it's a furnace\") and says nothing about cooling either way, so NEITHER the standalone-ducts member nor the shared-ducts-with-AC member is more supportable than the other. `member` names one of the two only because the format requires exactly one literal value in that slot — it is NOT a preference, and `acceptableAlternatives` proves it structurally: a model landing on either member scores identically (correct/sanctioned, never wrong), and only a member from a genuinely DIFFERENT equipment family (Boiler, Electric Resistance, a heat pump, ...) is a hard wrong answer. ANNOTATOR_GUIDE.md rule 6 states this as a mechanical rule, not a per-transcript judgment call: pick either member of the conflated pair, list the other as a sanctioned alternative, and say so — never invent a third, more-neutral answer, and never silently prefer one without saying they are symmetric."
      }
    },
    "hvac.hvacHeatingEnergySource": {
      "status": "asserted",
      "sourceSpan": "it's a furnace, a gas furnace",
      "member": "Natural Gas"
    },
    "attic.atticInsulationDepth": {
      "status": "declared_absent",
      "sourceSpan": "I'm giving you the R-value, not a depth"
    },
    "attic.atticInsulationType": {
      "status": "asserted",
      "sourceSpan": "fiberglass batts between the joists",
      "member": "Fiberglass or Rockwool (batts or blown)"
    },
    "attic.atticInsulation": {
      "status": "asserted",
      "sourceSpan": "R-13, final answer.",
      "value": 13,
      "retracted": [
        {
          "value": 11,
          "note": "\"I want to say R-11\" is retracted by \"no wait, these are the thicker ones, R-13 ... R-13, final answer.\" Last assertion wins."
        }
      ],
      "arguable": {
        "note": "Schema-rebuild note, not a corpus ambiguity: the pre-rebuild field set had no attic R-value write target at all and parked a spoken R-value in a fake `atticInsulationSpokenRValue` holding pen. The rebuilt schema.ts has a REAL `atticInsulation` field (a genuine write target, not a holding pen) — see schema.ts's HAZARD 2b note. This leaf is therefore asserted, not merely captured-for-later."
      }
    },
    "health.healthAmbientCarbonMonoxide": {
      "status": "asserted",
      "sourceSpan": "I did not do an actual ambient CO measurement at the appliance, so don't log that as a CO test",
      "member": "Not Tested",
      "arguable": {
        "acceptableAlternatives": [],
        "acceptableMiss": true,
        "note": "THE health trap this transcript is built around. A personal belt-clipped CO monitor that \"never went off\" is a tempting read as 'Passed', but the auditor explicitly says it is NOT a real test and instructs not to log it as one. 'Not Tested' (an explicit, addressed non-performance) is the intended reading — and this leaf's own explicit statement is MORE SPECIFIC than the generic 'No tests' disclaimer below, so it overrides that disclaimer's default for this one leaf (same resolved value here, but a more precise span). A model that leaves this leaf null is an acceptable-but-milder miss — the topic WAS addressed, so null undersells it, but it is not dangerous. 'Passed' is the specific, serious failure mode this leaf exists to catch: a model that stamps a pass on a test the auditor pointedly declined is a hallucinated pass, and must score as wrong, not as a sanctioned alternative — hence acceptableAlternatives is empty, not [\"Passed\"]."
      }
    }
  },
  "notes": {
    "attic.atticInsulationType": "'fiberglass batts' maps to the schema's combined 'Fiberglass or Rockwool (batts or blown)' member — the rebuilt schema does not separate fiberglass from rockwool the way the pre-rebuild field set's ENUMS list implied it might; there is only ever this one combined member to pick.",
    "hvac.hvacHeatingSystemModel": "The pipes described (\"all the pipes\", \"old abandoned hydronic\") belong to the RETRACTED boiler, not the furnace. No make/model/year is ever stated for the furnace itself — this is a genuinely empty leaf, not a dropped value.",
    "_general": "The other 12 health-matrix leaves besides ambientCarbonMonoxide (health.healthNaturalConditionSpillage through health.healthElectrical) are covered by the 'No tests' disclaimer above and resolve to asserted 'Not Tested' under the current disclaimer policy (disclaimer-policy.mjs), not to 'unmentioned'. This is now a POLICY decision applied uniformly across the corpus, not an annotator judgment call for this transcript — see ground-truth-format.md's Disclaimers section and disclaimer-policy.mjs's own header for how to change the policy later without re-annotating anything. health.healthRoofCondition is NOT part of the 'healthTests' named set (it's a visual condition judgement, not a pass/fail/warning/not-tested test) and is genuinely 'unmentioned' — the roof never comes up in this transcript at all."
  }
}
